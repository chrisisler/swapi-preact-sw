{
  "name": "swapi-preact-sw",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev",
    "build": "preact build",
    "serve": "preact build && preact serve",
    "dev": "preact watch"
  },
  "eslintIgnore": [
    "build/*"
    ],
    "devDependencies": {
      "if-env": "^1.0.0",
      "preact-cli": "^2.0.1"
    },
    "dependencies": {
      "preact": "^8.2.6",
      "preact-compat": "^3.17.0",
      "preact-router": "^2.5.7"
    }
}
