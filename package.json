{
  "name": "swapi-preact-sw",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev",
    "build": "preact build",
    "serve": "preact build && preact serve",
    "dev": "preact watch --port 9003"
  },
  "eslintIgnore": [
    "build/*"
  ],
  "devDependencies": {
    "babel-core": "^6.26.0",
    "babel-eslint": "^8.2.1",
    "eslint": "^4.15.0",
    "eslint-plugin-react": "^7.5.1",
    "if-env": "^1.0.0",
    "preact-cli": "^2.0.1",
    "preact-cli-plugin-async": "^1.0.0"
  },
  "dependencies": {
    "preact": "^8.2.6",
    "preact-compat": "^3.17.0",
    "preact-router": "^2.5.7"
  }
}
